# Static Site Export

This directory contains a pure HTML/CSS/JavaScript static site exported from the Pokemon TCG Search application.

## Files

- `index.html` - Main HTML file with all content
- `styles/` - CSS files for styling
- `scripts/` - JavaScript files for functionality
- `README.md` - Documentation

## Quick Start

1. Open `index.html` in a web browser, or
2. Serve using a local server:
   ```bash
   python3 -m http.server 8000
   ```
   Then visit `http://localhost:8000`

## Deployment

This static site can be deployed to any static hosting service without any build process.
