import React from 'react';
import ReactDOM from 'react-dom/client';
import { OCRSearch } from './pages/OCRSearch';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OCRSearch />
  </React.StrictMode>
);
